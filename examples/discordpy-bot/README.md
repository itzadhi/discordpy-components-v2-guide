# Discord.py Production Example Bot

This example uses discord.py 2.x, app_commands, cogs, views, modals, persistent view registration, and async service boundaries.

Run:

```bash
python -m venv .venv
python -m pip install -r requirements.txt
python -m py_compile src/bot.py
```
